# Run log, 2026-09-11 — what shipped, what is queued, and how to keep going

This run audited and extended the Erdős ore corpus. Everything below marked **shipped** carries a
verifier in this repository that exits 0. (One caveat for anyone re-running them in a batch:
`erdos1061_rays.py` needs several minutes — it computes σ over every partition of every s < 4000 —
and will hit a short per-script timeout. It passes given the time.) Everything marked **queued** was verified by a mining
pass but **not** independently recomputed by the author of this log — it is a lead, not a result,
and must be recomputed before publication.

---

## Shipped this run

| file | result |
|---|---|
| `ERDOS-170-SETTLED.md` | `c₀ = 1.5603`, not `√2` — the campaign's own frozen constant, evaluated at the wrong stationary point. Plus F(N) exhaustive to N=48, two corrected witnesses, the counting-bound tightness map |
| `ERDOS-1061-SIGMA-FAMILY.md` | `σ(a)+σ(2a)=σ(3a)` for every `a` coprime to 6, by multiplicativity. `S(x) ≥ 2x/9` |
| `ERDOS-1061-RAYS.md` | Every solution seeds a disjoint ray; 1,437 primitive seeds below 4000 sum to `liminf S(x)/x ≥ 0.948192`. The `2/9` family is the single largest ray |
| `TWO-PROOFS.md` | No odd unitary perfect number exists (four lines). Ω > k in every primorial window, explicit threshold `n > 2^k` |
| `TWO-CONSTRUCTIONS.md` | Erdős 655 refuted for every n by the regular n-gon. Erdős 477 closed for `k²`; the general-quadratic mechanism killed by `f(k)=2k²+k` |
| `ERDOS-396-WITNESS.md` | **`8178·8177·8176·8175 | C(16356,8178)`** — the only witness below 30,000, outside the corpus's search bound. Plus a kernel receipt that certifies `True` |
| `DIGIT-CRITERIA.md` | Erdős 376: exactly twelve witnesses to `10⁷` against a filed 1,295. Erdős 1142: a sieve 2.88× stronger |
| `SIX-REFUTATIONS.md` | `ex(6,K₂,₂,₂) = 13` with 60 extremizers. Plus 317, 295, 479, 156, 683 |
| `FOUR-CORRECTIONS.md` | Erdős 386 `n=715` (six consecutive primes), 932 `r=4`, and two retractions that were themselves wrong |
| `FOUR-OBJECTS.md` | (1728,1764,1800) consecutive powerful AP; 1107 gaps; 949 sharpness; 913 witness |
| `MEASURED-VALUES.md` | 81770 with eight prime-square representations; `N(10⁷)=6079291`; and four more |

---

## Queued — verified by a mining pass, NOT independently recomputed

**Recompute before publishing.** Grouped by how cheap the check is.

### Cheap (finite tables, minutes)

- **E36** `M(1..8) = 1,1,2,2,3,3,3,4` with extremizers — M(5)–M(8) reportedly new
- **E82** `F(1..7) = 1,2,2,2,3,3,4`; F(7) witness `K₄ + 3 isolated vertices`
- **E85** `f(4..12) = 2,3,3,3,3,3,4,4,4`; f(12)=4 via the truncated tetrahedron
- **E156** min maximal Sidon `m(1..12) = 1,2,2,2,3,3,3,3,3,3,4,4` (convention-sensitive)
- **E535** `f₃(1..14) = 1,2,2,3,3,3,3,4,5,5,5,5,5,5`; the dyadic bound is **not tight at N=9**, witness `{2,3,4,8,9}`
- **E624** `H(8) = 4` exactly; H(7) unsettled
- **E680** complete failure set `{1,3,7,13,23,31,113,115}` to `10⁶`
- **E859** exact densities `d₁..d₁₀ = 1, 1/2, 2/3, 1/2, 7/15, 7/15, 16/35, 3/7, 17/45, 2/5`
- **E1106** `F(1..20)`; `F(n) > n` first at `n=116`
- **E1210** exact deficits `D(n)` for `n=2..14`; max 1 at `n ∈ {2,3,4,6}`
- **E950** `f(9) = 89/84`; `Σ_{n≤N} f(n) = Σ_{p<N} H_{N−p}`

### Moderate (search, tens of minutes)

- **E213 minimum diameter of integral point sets in general position.** Reported: n=4 → 8, n=5 → **73**, n=6 → **174**, with witnesses in `ℚ(√55)` and `ℚ(√2002)`. *The author verified the witnesses are genuine integral sets in general position; minimality was NOT independently checked, and an attempt to re-derive it hit three separate bugs (rational-vs-integer distance, unenforced diameter, missing concyclicity). Treat minimality as unverified.* Note `√2002` also appears in the estate's published `kreisel-kurz-heptagon-extension`.
- **E1060** `max_{n≤10¹²} f(n) = 4`, first at `n = 5,418,319,872` (k = 41664, 42672, 47244, 55118)
- **E1093** max deficiency **9** at `(n,k) = (284,28)`, against a filed max of 3
- **E1094** exception census `(7,3), (13,4), (14,4), (23,5), (44,8), (46,10), (47,10), (47,11), (62,6), (74,10), (94,10), (95,10)`
- **E681** failure counts `9 / 61 / 149 / 943 / 5603 / 31284` at `24 / 306 / 10³ / 10⁴ / 10⁵ / 10⁶`
- **E1142** Good set empty on `(105, 10⁷]`

### Structural (need a written argument)

- **E600** `e(n,2) = 4,4,7,7,10` for n=3..7; extremal graphs are friendship graphs. The filed `e(n,2) = 1 + D(n,3,2)` conflates edges with triples
- **E891** the **ω** reading is a different and open problem: for k=2 it fails at 69 values of `n ≤ 2·10⁵`, largest `n = 4372`
- **E509** minimal symmetric two-disc cover of `{|z²−1| ≤ 1}` has total `√2`, not the filed `9√2/8`
- **E513** `M(1)² = (207+33√33)/128` and `ρ* = 1/M(1)` exactly — a PROVED row retracts this correct identity on a miscomputed `M(1)²`

---

### Added by the final six passes — highest value first

All **unrecomputed by this author**. Ordered by how much they would change a published record.

1. **#539 — `h(n) = n` is false from n = 7, and the truth is `O(n^{2/3})`.** The ore files
   `h(n) = n` as PROVED. Counterexample reported: `A = {2,3,4,6,9,12,18}` (divisors of 36 except
   1 and 36) gives `|Q(A)| = 6 < 7`. The extremal shape is reported as an anti-diagonal band of a
   two-prime divisor lattice, giving `h(n) ≤ (3/2+o(1))(2n)^{2/3} ≈ 2.381 n^{2/3}`. If it holds,
   this is the largest single correction found in the corpus. **Check this first.**
2. **#930 — k=3 and k=4 disjoint-interval square products.** Reported: `{5,6,7}×{14,15,16} =
   840²`, and `{33,34,35,36}×{1680,…,1683} = 3361826160²` with *both* intervals of length 4. The
   ore files "k=4 nonexistence certified for t ≤ 10⁶" four separate times — from a search that
   anchored `I₁ = {1,2,3,4}` and varied only the second interval.
3. **#396 — the k=5 least witness, reported as n = 3,648,841.** Would extend the published table
   one more level. Also reported: a √(2n)-smoothness lemma that makes the search tractable.
4. **#1107 — the complete exception set is `{7, 15, 23, 87, 111, 119}`.** This repository
   publishes `{7,15,23,87}` below 100; 111 and 119 lie above that cutoff. Two independent passes
   agree on the full set to 10⁶ and 10⁷.
5. **#390 — a three-way contradiction settled: `f(7) = 20`, `f(8) = 16`.** The ore carries
   f(7) = 20, 24 and 21 in different rows, and f(8) = 16 vs 36.
6. **#1094 — the complete 14-element exception set** for `lpf C(n,k) ≤ max(n/k,k)`, largest at
   `(284,28)`. Two independent passes agree.
7. **#213 — the minimal integer-coordinate witness is `(0,0), (6,0), (3,4), (3,−4)`,** diameter 8,
   distances `{5,5,5,5,6,8}`. Generalizes: any Pythagorean triple gives a rhombus with diagonals
   `2a, 2b` and sides `c`.
8. **#503 — `f(2) ≥ 6`**, via the regular pentagon *plus its centre* (all 20 triples isosceles).
   The ore files 5.
9. **#689 — the true threshold is 139, not 300.** `Σ_{p≤n}⌈n/p⌉ ≥ 2n` fails exactly on
   `{1,…,136} ∪ {138}`; n = 137 is tight at exactly 2n.
10. **#170 — `F(N)` exact to N = 55**, and the parity obstruction generalized to
    `F(C(k,2)) ≥ k+1` for `k ≡ 5, 7 (mod 8)`. Independently matches the `k = 5, 7, 13`
    generalization published here.
11. **#979 — `f₂ = 13` at `n = 9,549,410`**, extending the `f₂ = 8` at 81770 published here.
12. **#600 — `D(n)` extended to n = 11**: `3,3,6,6,9,12,18,18,21` for n = 3…11.

### Cross-problem contamination, worth knowing before trusting any import

A pass over the `transfer-evidence` files reports that **unsound imports out-propagated their own
corrections**. Two specimens:

- An #655 → #653 import applies a true statement about the regular n-gon to conclude `g(n) = 1`,
  bounding a **maximum over configurations** by exhibiting one configuration. It reached 8
  destinations; the same campaign's correct diagnosis of the quantifier slip reached 10, and three
  problems received **only** the unsound version.
- #383 exported **both** a true row (p=7 refutes "p²+1 always has a prime factor > p") and its
  negation. Two problems received both.

Treat any imported lemma as true-where-proved until checked in the destination's hypotheses.

## Method that worked

1. **Recompute before writing.** Every publishable result here came from disagreeing with a filed number. Several near-misses were caught only because the author computed first — most sharply, an elegant Pell identity (`8119² − 2·5741² = −1`) that is *true* but attached to the wrong question.
2. **Extend the range.** The cheapest new mathematics available. A divisibility filed "no witness for n < 6000" had one at `n = 8178` under the identical criterion. A census filed for "prime blocks P ≤ 30020" missed `C(715,2) = 255255`.
3. **Check the reduction's direction.** "Contains" vs "is a subgraph of" changed an extremal number from 13 to 12. Bounding `F` needs the lcm from *below*.
4. **Ask what a witness constrains.** Several filed witnesses have the other arm of a `min` already satisfied and constrain nothing.

## Six failure modes, measured

1. **Claims failing inside their own stated range** — "all n ≤ 2000" failing at 341, the smallest Fermat pseudoprime, known since 1819.
2. **Enumerations claimed but not run** — a "3ⁿ exhaustive search" whose answers are exactly what adjacent-pairs-only gives.
3. **Retractions that are themselves wrong** — every retraction examined this run withdrew a *correct* result: `p=47` (on `47²+1 = 2222`, which is `47²+13`), `K(97;6)`, `f(22)`, `ρ*`.
4. **The wrong object certified** — a triple product for a pair predicate; `x+y≡0` for `x·y≡1`; `P ∨ ¬P` for `R(3,3) ≤ 6`.
5. **An asymptotic estimate filed as an exact count** — `N(10⁷)` recorded as `⌊6·10⁷/π²⌋`, "cross-checked" twice.
6. **Same-campaign contradictions decidable in seconds** — one row holding the counterexample to the PROVED row beside it.

None of this touches the mathematics underneath. The corpus's *specific* results survive recomputation far more often than its *generalizations* do.

## Scale

`4,204` rows carry status `PROVED` or `COMPUTATION_SUPPORTED` across 225 campaigns. Every one has
now been read. Of the Day-1 archive, `7,536` unique files (92.7%, ~120 MB) had never been written
to disk before this run — twelve nested zips were never descended into.

Two counts in the corpus are `wc -l` artifacts of embedded newlines: "15,027 implication edges" is
**789** rows; "1,469 revival requirements" is **130**.
